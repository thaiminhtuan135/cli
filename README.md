# install python3

## Prerequisites

- Install Python 3

### Installation

1. **Clone the Repository**

   ```bash

   git clone https://github.com/thaiminhtuan135/cli.git
   cd vtid3
   
   pip install --upgrade setuptools
   pip install -r requirements.txt
   python setup.py install
   pip install .